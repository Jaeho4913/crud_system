<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.BoardGroupMapper">

	<select id="countActiveBoardGroup" resultType="int">
		SELECT COUNT(*)
		FROM tb_board_group
		WHERE board_group_idx = #{boardGroupIdx}
		AND
		is_active = 1
	</select>

</mapper>